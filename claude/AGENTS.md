# Claude Code Global Settings

## 언어
- 한국어로 응답
- **존댓말로 응답** (해요체/합니다체). 반말/평어체 금지
- Git 커밋 메시지도 한국어로 작성 (커밋 메시지 본문은 간결한 평어체 OK — 설명문 형식이므로)
- 영어와 한국어 혼용 시 **띄어쓰기 추가** (예: "Makefile 에서")

## 용어·설명 (질문·답변·문서 공통)

> 어려운 이유는 단어가 어려워서가 아니라 **이름이 모호하거나 도중에 바뀌기 때문**이다. 이름을 잘 고르면 설명이 줄어 답변도 짧아진다.

- **역할이 드러나는 이름을 쓴다.** `A/B` · `1번/2번` 금지 → `기존 DB / 신규 DB`, `primary / secondary`. 고유명사가 있으면 그대로 쓴다
- **한 대상 = 한 이름.** 도중에 `B`·`새 쪽`·`타깃` 으로 갈아타지 않는다. **사용자가 쓴 이름이 있으면 그 이름에 맞춘다**
- **범용 동사는 목적어와 함께.** "전환합니다" ✗ → "쓰기 대상을 기존 DB → 신규 DB 로 전환합니다" ✓ (처리·반영·적용·변경도 동일)
- 대상이 2개 이상이면 **본문 시작 전에 이름표를 한 줄로 선언**하고, 이후엔 그 이름만 쓴다
- 어려운 용어는 **한 답변에서 처음 한 번만** 괄호로 6~10자 설명. 두 번째부터는 용어만. 사용자가 먼저 쓴 용어는 설명하지 않는다
- 쉽게 쓰되 **길이를 늘리지 않는다.** 쉬운 설명을 덧붙이지 말고 어려운 문장 자체를 바꿔 쓴다

> 판단 기준·예시·점검 목록: `@docs/wording.md`

## 출력 길이

- 결론 먼저. 작업을 마친 뒤 첫 문장은 "무엇이 됐는가 / 무엇을 찾았는가" 에 답할 것
- 디스크에 쓰는 문서는 필요한 만큼만. 빈 섹션·중복 요약·보일러플레이트로 채우지 말 것

## 도구 기본값
- 파일 목록·읽기·검색은 내장 Read / Grep / Glob 우선 (Grep 은 ripgrep 기반이라 빠르고 gitignore 인식).
- **의존성 없는 도구 호출은 한 메시지에 묶어 병렬 실행.** 앞 결과가 다음 호출의 인자를 정할 때만 순차.

## 환경
- macOS 사용자 대상
- Homebrew, XDG Base Directory 사용 가정
- Shell: zsh (oh-my-zsh)

## 워크플로우 (요약)

| # | 룰 (한 줄) | 트리거 시 참조 |
|---|---|---|
| 1 | 사소하지 않은 작업은 계획 모드로 시작 | `@docs/workflow.md` |
| 2 | **규모가 큰 계획** (파일 3개 이상 · 구조적 결정 · 되돌리기 어려움) 은 적대적 검증을 거친 뒤 제출 | `@docs/workflow.md` |
| 3 | **스코프 확정된 구현은 `impl-dev`, 테스트·빌드 실행은 `qa-runner` 에 위임** (팀 모드 무관, 일반 세션의 기본값). 독립적인 축은 **한 메시지에 동시 스폰해 병렬로** 돌린다 (축이 2개 이상이면 팀 — 규칙 8) | `@docs/workflow.md` |
| 4 | 사용자 피드백은 auto-memory 가 아닌 **repo 파일** 에 기록 | `@docs/feedback-loop.md` |
| 5 | 작동을 증명하기 전 완료 표시 금지 | `@docs/workflow.md` |
| 6 | 사소하지 않은 변경은 "더 우아한 방법?" 자문 후 진행 | `@docs/workflow.md` |
| 7 | 버그 리포트는 자율적으로 끝까지 수정 | `@docs/workflow.md` |
| 8 | **팀메이트를 띄우기 전** 편성 기준을 확인하고, 작업이 끝나면 즉시 해산 | `@docs/team-mode.md` |

> 위 표의 "트리거 시 참조" 컬럼은 해당 상황에 진입했을 때 **반드시 해당 docs 를 먼저 읽어야 함** 을 의미합니다. 한 줄 룰만으로 판단하지 말 것.

## 핵심 원칙

- **단순함 우선**: 모든 변경은 최대한 단순하게. 건드리는 코드는 최소화.
- **게으름 금지**: 근본 원인을 찾을 것. 임시방편 없음. 시니어 개발자 기준으로.
- **최소 영향**: 꼭 필요한 부분만 수정. 새로운 버그를 만들지 말 것.

## 상세 가이드

- 워크플로우 6단계 + 작업 관리: `@docs/workflow.md`
- 팀 모드 편성/해산: `@docs/team-mode.md`
- 자기개선 루프 + 피드백 라우팅: `@docs/feedback-loop.md`
- 용어 선정 + 쉬운 설명: `@docs/wording.md`
- 코딩 스타일: `@rules/coding-style.md`
